# Required. Clerk publishable key, from the Clerk dashboard (API Keys).
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here

# Optional. Base URL of the backend API. Defaults to http://localhost:8000 if unset.
VITE_API_URL=http://localhost:8000
